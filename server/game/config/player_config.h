#ifndef SERVER_CONFIG_PLAYER_CONFIG_H
#define SERVER_CONFIG_PLAYER_CONFIG_H

struct PlayerConfig {
    float max_velocity;
    float acceleration;
    float radius;
    int starting_money;
    int max_health;
};

#endif
